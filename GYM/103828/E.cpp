#include <bits/stdc++.h>

using namespace std;

int main() {
    freopen("foo.txt", "r", stdin);
    int tc; cin >> tc;
    while(tc--) {
        int n; cin >> n;
        cout << n - 1 << endl;
    }

    return 0;
}
