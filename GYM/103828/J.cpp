#include <bits/stdc++.h>

using namespace std;

int main() {
    freopen("foo.txt", "r", stdin);
    int tc; cin >> tc;
    while(tc--) {
        int n; cin >> n;
        for(int i = 1; i <= n; i++)
            cout << i << ' ';
        cout << endl;
    }
}

