#include <bits/stdc++.h>

using namespace std;
#define int long long

const int N = 1e3 + 5;
int n, k, ans[N];

void solve() {
    string s;
    cin >> s >> n >> k;
    k--;
    if (k == 0) {
        cout << s << endl;
        return;
    }
    for (int i = 1; i <= n; ++i) {
        for (int )
    }

}

int main() {
    ios_base::sync_with_stdio(0); cin.tie(0);

    int tc;
    cin >> tc;
    while (tc--) {
        solve();
    }
}
