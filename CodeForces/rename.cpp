#include <bits/stdc++.h>
#include <windows.h>
using namespace std;

int main() {
	// string prob, contest, ext;
    // WIN32_FIND_DATA data;
    // HANDLE hFind = FindFirstFile("D:\\CodeHub\\CodeForces/", &data);  // DIRECTORY

    // if ( hFind != INVALID_HANDLE_VALUE ) {
    //   do {
    //     // std::cout << data.cFileName << std::endl;
    //   	string tmp = data.cFileName;
    //   	cerr << tmp << "\n";
    //   	// if (tmp.size() < 4 || tmp[tmp.size() - 4] != '.') continue;
    //   	// prob = tmp.substr(0, tmp.size() - 4);
    //   	// ext = tmp.substr(tmp.size() - 3, 3);
    //   	// cerr << prob << " " << ext << "\n";
    //   	// for (int i = 0; ; i ++) {
    //   	// 	cerr << prob[i] << "\n";
    //   	// 	if (isalpha(prob[i])) {
    //   	// 		contest = prob.substr(0, i);
    //   	// 		cerr << "contest: " << contest << "\n";
    //   	// 		prob = prob.substr(i, prob.size() - i);
    //   	// 		cerr << "prob: " << prob << "\n";
    //   	// 		break;
    //   	// 	}
    //   	// }
    //   	// // cerr << "mkdir D:\\CodeHub\\CodeForces\\" + contest << "\n";
    //   	// system(("mkdir D:\\CodeHub\\CodeForces\\" + contest).c_str());
    //   	// // cerr << "ok\n";
    //   	// system(("copy D:\\CodeHub\\CodeForces\\" + tmp + " D:\\CodeHub\\CodeForces\\" + contest + "\\" + prob + "." + ext).c_str());
    //   } while (FindNextFile(hFind, &data));
    //   FindClose(hFind);
    // }
}