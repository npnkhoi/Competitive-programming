using namespace std;

int main() {
    int n;
    scanf("%d", &n);
    for (int i = 1; i <= n; i++) scanf("%d", &a[i]), s[a[i]] ++;
    for (int i = 1; i <= n; i++) scanf("%d", &n[i]), s[b[i]] ++;

    for (int i = 1; i <= n; i++)
}
